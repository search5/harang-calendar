# Manual test checklist: note integration

Tracks manual verification in the real Obsidian app (the automated test
suite only covers pure logic - date/query math, regex matching order,
frontmatter position parsing - not actual rendering/interaction). Check
items off as they're confirmed in the test vault.

## `@date[` / `@event[` autocomplete

- [x] `@date[2026-07` 입력 → 팝업 뜨는지, 입력할수록(`08`→`0807`) 후보가 좁혀지는지
- [x] `@date[` 만 입력(쿼리 없음) → 오늘부터 며칠치가 뜨는지
- [x] 후보 선택 → `[[cal:2026-08-07]]`로 정확히 치환되고 `]`가 중복/누락 없이 깔끔한지
- [x] `@event[회의` 처럼 제목 일부(공백 포함 여러 단어)로 검색되는지
- [x] 후보 선택 → `[[event:<uid>]]`로 치환, 괄호 안 남는지

## 라이브 프리뷰 렌더링

- [x] `[[cal:...]]` → 커서가 벗어나면 그날 일정 카드 위젯으로 바뀌는지
- [x] `[[event:...]]` → 인라인 칩으로 바뀌는지
- [x] 커서가 그 참조 안에 있을 때는 위젯/칩이 아니라 원문 텍스트 그대로 편집 가능한지
- [x] 코드블록/인라인 코드 안에 같은 문자열을 써도 렌더링되지 않는지

## 읽기 모드(Reading view)

> 🐛 버그 발견 및 수정: 읽기 모드에서 칩/위젯이 렌더링되지 않고 일반 위키링크로 풀려서 보임 — Obsidian이 포스트프로세서가 돌기 전에 `[[...]]`를 위키링크(`<a class="internal-link">`)로 먼저 파싱해버려서, 원문 텍스트에서 패턴을 찾던 기존 로직이 아무것도 못 찾고 있었음. 위키링크로 변환된 `<a>` 태그의 `href`도 같이 검사하도록 수정함(`postProcessor.ts`). 수정 후 재확인 완료.

- [x] 편집 모드 → 읽기 모드 전환 시 위젯/칩이 동일하게 보이는지
- [x] 코드블록 안 텍스트는 읽기 모드에서도 그대로인지

## 날짜 위젯 상호작용

> ✅ CDP로 실제 실행 중인 Obsidian(볼트 "Obsidian")에 접속해 직접 확인함 (2026-07-27).

- [x] 위젯 안 이벤트 클릭 → 상세 팝업(일시/장소/메모/캘린더명) — 클릭 시 팝업이 뜨고 일시/캘린더명이 정확히 표시됨(해당 이벤트는 장소/메모가 없어 그 필드는 생략됨 — 값이 있을 때만 렌더링하는 게 의도된 동작).
- [x] 그 날 일정이 없으면 "일정 없음" 문구 — "이 날짜에는 일정이 없습니다." 정상 표시.
- [x] 반복 일정이 해당 날짜에 정상적으로 나타나는지 — 서버에 실제 반복 일정이 없어 `calendarStore`에 매주 반복(`FREQ=WEEKLY;COUNT=5`) 합성 이벤트를 주입해 확인. 매 발생일(8/11, 8/18 등)에 정확한 시간으로 위젯에 나타남.
- [x] all-day 이벤트가 정확히 그 날짜에만 나타나는지(하루 밀리지 않는지) — 아래 버그 수정 후 재확인 완료.

> 🐛 **버그 발견 및 수정 (2026-07-27, CDP 테스트 중 실 데이터로 발견)**: 명시적 종료일이 있는 종일(all-day) 이벤트가 실제로는 마지막 날 다음날까지 위젯에 나타남 (예: 8/7 하루짜리 이벤트가 8/7과 8/8 둘 다에 표시됨). `store.ts`의 `overlapsRange`/`eventEndMs`가 `YYYY-MM-DD` 문자열을 `new Date(str)`로 파싱해 UTC 자정으로 취급했는데, 로컬로 구성한 조회 범위와 어긋나 UTC보다 앞선 시간대(예: Asia/Seoul, UTC+9)에서 하루 밀림 — `agenda.ts`/`monthGrid.ts`의 표시 레이어에서는 이미 고쳤던 것과 같은 종류의 버그가 `store.ts`의 쿼리 필터링 레이어에는 독립적으로 남아있었음. `parseEventBoundary` 헬퍼를 추가해 종일 이벤트는 로컬 달력 구성요소로 파싱하도록 수정. 3개 시간대(Asia/Seoul, America/New_York, UTC)에 대한 유닛 테스트로 검증 후, 실행 중인 Obsidian에 핫리로드해 실 데이터로 재확인 완료 — 수정 후 해당 이벤트가 8/7에만 나타남.

## 이벤트 칩 상호작용

- [x] 칩 클릭 → 상세 팝업, Esc로 닫힘, 팝업 바깥 클릭으로도 닫힘 — CDP로 클릭/Escape 키 이벤트를 직접 발생시켜 확인.
- [x] 존재하지 않는 uid(`[[event:nope]]`를 **대괄호 두 개로 직접** 입력) → 점선/반투명 칩 + "찾을 수 없음" 팝업 — `harang-calendar-event-chip-unresolved` 클래스 확인, 클릭 시 "이 이벤트를 찾을 수 없습니다. 새로고침이 필요할 수 있습니다." 팝업 정상 표시.

## 프론트매터 연동

> ⚠️ **설계 변경 (2026-07-28, 두 번)**: ① `harang-calendar` 프론트매터가 캘린더 표시 이름 배열(`[Work, Personal]`)에서 CalDAV **계정** 이름 단일 문자열로 바뀜 → ② 한 계정 아래 캘린더를 2개 만들어보니 계정만으로는 구분이 안 돼서, `harang-account`(계정 이름)/`harang-calendar`(캘린더 이름, 원래 의미로 복귀) **두 개의 독립 키**로 다시 나눔 — 둘 중 하나만 있으면 그 값 하나로(계정 전체 또는 그 이름의 캘린더 전체 계정 통틀어), 둘 다 있으면 그 계정 안의 그 캘린더 하나로 좁혀짐. 자동완성은 여전히 없음(설정에서 복사/붙여넣기 또는 직접 입력 전제) — 관련 자동완성 코드(`FrontmatterCalendarSuggest.ts`, `frontmatterQuery.ts`, `frontmatterPropertySuggest.ts`)는 삭제됨. 자세한 경위는 ROADMAP.md 참고.
>
> ①(계정 단독 스코핑) 시점에 CDP로 맞는 이름/틀린 이름 두 경우 다 확인 완료(이벤트 표시 / 0개+경고). ②(`harang-account`/`harang-calendar` 분리) 이후로는 아직 재검증 전 — 아래를 새로 확인해야 함:
> - [ ] `harang-account`만 있을 때 → 그 계정의(활성화된) 모든 캘린더로 범위 제한되는지 (①에서 확인한 것과 같은 코드 경로라 위험도는 낮음)
> - [ ] `harang-calendar`만 있을 때 → 그 이름의 캘린더로 범위 제한되는지(여러 계정에 동명 캘린더가 있으면 전부 매칭되는지까지)
> - [ ] 둘 다 있을 때 → 그 계정 안의 그 캘린더 하나로만 좁혀지는지(같은 계정의 다른 캘린더 이벤트는 안 보이는지)
> - [ ] 오타 등 등록되지 않은 값을 넣으면 위젯에 경고가 뜨는지(`dateWidgetUnknownScope`) — 계정/캘린더 이름 각각 따로, 그리고 둘 다 틀렸을 때 두 이름이 같이 뜨는지
> - [ ] 프론트매터(`harang-account`/`harang-calendar` 둘 다)가 없으면 구독 중인 전체 계정/캘린더 대상인지
> - [ ] 설정 패널에서 계정 이름 입력창에 새로 추가한 설명(`settingsAccountNameDesc`)이 잘 보이는지

<details>
<summary>이전 배열+자동완성 설계 테스트 기록 (지금은 대부분 무의미, 참고용으로 보존)</summary>

- [x] `harang-calendar: [실제 등록된 캘린더 이름]` 추가 후 본문 위젯이 그 캘린더로만 범위 제한되는지 — 정상 동작(이벤트 그대로 표시, 경고 없음).
- [x] 오타 등 등록되지 않은 캘린더 이름을 넣으면 위젯에 경고가 뜨는지 — 정상 동작. 단, **완전히 새로 렌더링된 경우에만** 확인됨: 프론트매터만 바꾸고 본문(`[[cal:...]]`) 텍스트는 그대로 둔 채 같은 읽기 모드 뷰를 유지하면 Obsidian이 그 줄의 렌더링을 재사용해 옛 결과가 그대로 남아있는 현상을 발견함(리프를 닫았다 다시 열면 즉시 반영됨). 이건 우리 코드의 버그가 아니라 Obsidian 자체 프리뷰 diffing이 프론트매터 변경을 인지하지 못하는 것으로 보임.
- [x] 프론트매터 없으면 구독 중인 전체 캘린더 대상인지 — 기존 테스트 노트(프론트매터 `harang-calendar:` 빈 값)에서 이미 계속 확인됨.
- [x] Properties 패널(GUI 속성창)에서도 "harang-calendar" 속성 값을 입력할 때 같은 자동완성이 뜨는지 — 실사용자 테스트에서 여러 번 재현되는 상호작용 버그(아래) 끝에, 결국 Obsidian의 `AbstractInputSuggest`를 쓰던 걸 자체 구현 팝업(`InlineCalendarSuggest`)으로 교체해서 해결. 최종적으로 타이핑→클릭→타이핑→클릭(전체 사이클 2회 연속)을 실제 클릭(CDP `Input.dispatchMouseEvent`)으로 검증 — 매번 팝업 1개만 뜨고, 클릭 한 번에 정확히 선택되고, 프론트매터에 중복/오타 없이 정상 저장됨.
  - 버그 1: 선택 후 남은 텍스트를 `execCommand("delete")`로 지웠는데, 이게 진짜 `input` 이벤트를 발생시켜 자동완성 자신의 리스너를 재트리거 → 빈 검색어로 팝업이 재오픈됨. `textContent = ""` 직접 대입으로 수정.
  - 버그 2: 플러그인을 여러 번 껐다 켜면 같은 입력창에 서제스트 인스턴스가 쌓이는 버그. `dataset` 플래그로 막으려 했으나 Obsidian이 그 행을 다시 그릴 때 같이 지워버려 무력화 → `Symbol.for(...)` 키의 JS 객체 속성으로 교체.
  - 버그 3: `selectSuggestion`에서 선택 후 `this.inputEl.focus()`를 다시 호출한 게 자동완성 자신의 "포커스 시 팝업 열기" 리스너를 재트리거. `focus()` 호출 제거로 해결 시도.
  - 버그 4: 버그 3 수정 후에도 두 번째 타이핑→선택 사이클에서만 재현되는 팝업 중복이 남아있었음 — CDP로는 전혀 재현이 안 되고 실제 사용자 조작에서만 재현. `AbstractInputSuggest` 사용을 완전히 걷어내고 자체 구현 팝업으로 교체한 뒤 재현 안 됨.
  - 버그 5: "가장 처음"(완전히 새 속성, Text 타입)에 선택하면 데이터는 저장되지만 Properties 패널이 Text→List 타입 전환을 화면에 반영하지 않아 "선택이 안 된 것"처럼 보임. `app.metadataTypeManager.setType()`으로 강제 전환 시도했다가 **방금 저장한 값이 `null`로 날아가는 파괴적 부작용**을 발견하고 포기. 대신 `processFrontMatter` 대신 `editor.replaceRange`로 프론트매터 블록만 국소 수정하는 방식으로 교체, 입력창이 포커스된 상태면 여전히 반영이 안 되는 것까지 추가로 발견해 `select()`에서 쓰기 직전 `inputEl.blur()` 호출.

</details>

## 엣지 케이스

- [x] `[[cal:2026-02-30]]`처럼 실존하지 않는 날짜를 손으로 입력 → 위젯 대신 원문 그대로 남는지 — 정확히는 "원문 텍스트"가 아니라 Obsidian 자체의 (미해결) 위키링크로 남음(`<a class="internal-link is-unresolved" href="cal:2026-02-30">`) — `isValidIsoDate` 검사에서 걸러지면 우리 위젯으로 바꾸지 않고 그대로 두기 때문에 Obsidian의 기본 처리에 위임되는 게 의도된 동작이고 실제로 그렇게 동작함.
- [x] 한 줄에 날짜 참조와 이벤트 참조가 섞여 있을 때 순서대로 잘 렌더링되는지 — DOM 순서 직접 확인: 텍스트("before ") → 칩 → 텍스트(" middle ") → 위젯 → 텍스트(" after"), 정확히 원문 순서와 일치.
- [ ] (알려진 제한사항) 창을 분할해서 여러 노트를 동시에 열어둔 상태에서 프론트매터 스코프가 "현재 활성 파일" 기준이라 어긋날 수 있음 — CDP 세션에서는 다루지 않음(단일 창 기준 테스트만 수행). 필요하면 직접 확인 요망.

## 노트를 캘린더 뷰에 표시 (`harang-date`/`harang-repeat`/`harang-time`)

> ✅ CDP로 실제 실행 중인 Obsidian에서 확인함.

- [x] `harang-date`만 있는 노트가 사이드바 아젠다 뷰의 해당 날짜 아래 나타나는지 — 정상 확인(단발성 테스트 노트, 7/30에 정확히 표시).
- [x] `harang-date` + `harang-repeat: FREQ=WEEKLY;COUNT=4`가 매주 반복 전개되어 4번 다 나타나는지 — 정상 확인(7/28, 8/4, 8/11, 8/18 전부 정확히 표시, 실제 CalDAV 일정과도 올바르게 섞여서 날짜순 정렬됨).
- [x] 사이드바 아젠다 뷰에서 노트 일정을 클릭하면 그 노트가 열리는지(이벤트 팝업이 아니라) — 정상 확인.
- [x] 월간 뷰(그리드 안 알약 + 선택한 날짜 아래 목록)에서도 노트 일정이 같은 방식으로 나오고 클릭하면 노트가 열리는지 — 정상 확인.
- [x] 노트 일정이 실제 CalDAV 일정과 시각적으로 구분되는지(점선 테두리) — CSS 클래스(`harang-calendar-note-event-card`/`harang-calendar-month-day-pill-note`) 적용 확인.
- [x] (부수적으로 발견 및 수정) 사이드바 아젠다 뷰의 CalDAV 일정에 원래 클릭 핸들러가 아예 없었던 버그 — 이번에 고치면서 같이 확인, 클릭 시 정확한 일시/캘린더명으로 팝업이 뜸.
- [x] `harang-date` + `harang-time: 14:00-15:00`이 있는 노트가 사이드바/월간 뷰 모두에서 "14:00–15:00"으로 표시되는지 — 정상 확인(오늘 날짜에 등록, 두 뷰 모두 동일하게 표시).
- [x] `harang-time`이 있는 노트가 같은 날짜의 시간 있는 실제 CalDAV 일정과 시간순으로 올바르게 섞여 정렬되는지 — 정상 확인.
- [x] `harang-time` 없이 `harang-date`만 있으면 기존처럼 종일로 표시되는지(회귀 없음) — 정상 확인.
- [x] 잘못된 `harang-time` 값(`not-a-time`)을 넣었을 때 에러 없이 종일로 대체 표시되는지 — 정상 확인.
- [ ] `harang-date`가 이미 지난(과거) 날짜일 때 아젠다 뷰(다가오는 30일)에는 안 나타나는지 — 논리상 당연히 그래야 하지만 CDP로 직접 확인은 안 함.
- [ ] 잘못된 `harang-repeat` 문법(파싱 실패)을 넣었을 때 조용히 무시되는지(에러로 죽지 않는지) — 코드상 `try/catch`로 처리되지만 CDP로 직접 확인은 안 함.
